---
const {
  variant,
  size,
  onlyIconSize,
  gap,
  href,
  class: className,
  ...rest
} = Astro.props;
// Default button
const defaultClass = [
  "text-white ",
  "bg-content-gray",
  "hover:bg-base-700",
  "ring-content-gray",
  "hover:ring-base-800",
];
// Accent button
const accentClass = [
  "text-black ",
  "bg-mid-blue",
  "hover:bg-mid-blue",
  "ring-mid-blue",
  "hover:ring-accent-blue",
];
// Muted button
const mutedClass = [
  "text-content-gray ",
  "bg-white",
  "ring-base-200",
  "hover:ring-content-gray",
];
// Size (only applies if variant is not "link")
const xs = ["h-8", "px-3.5", "py-2", "text-xs"];
const sm = ["h-9", "px-4", "py-2", "text-sm"];
const base = ["h-10", "px-4", "py-3", "text-base"];
const md = ["h-11", "px-6", "py-3", "text-md"];
const lg = ["h-12", "px-6", "py-3", "text-lg"];
const xl = ["h-14", "px-6", "py-3", "text-lg"];
// Gap
const gapXS = ["gap-2"];
const gapSM = ["gap-4"];
const gapBase = ["gap-8"];
const gapMD = ["gap-10"];
const gapLG = ["gap-12"];
const additionalClasses = className ? className.split(" ") : [];

const Tag = href ? 'a' : 'button';
---

<Tag
  href={href}
  class:list={[
    "flex",
    "text-center",
    "font-medium",
    "items-center",
    "ring",
    "focus:z-1",
    "focus:ring-2",
    "focus:ring-inset",
    "justify-center",
    "duration-300",
    "ease-in-out",
    "transition-all",
    variant === "default" && defaultClass,
    variant === "accent" && accentClass,
    variant === "muted" && mutedClass,
    variant === "none" && [], // No additional classes for "none" variant
    size === "xs" && xs,
    size === "sm" && sm,
    size === "base" && base,
    size === "md" && md,
    size === "lg" && lg,
    size === "xl" && xl,
    gap === "xs" && gapXS,
    gap === "sm" && gapSM,
    gap === "base" && gapBase,
    gap === "md" && gapMD,
    gap === "lg" && gapLG,
    ...additionalClasses,
  ]}
  {...rest}
>
  <slot name="left-icon" />
  <slot />
  <slot name="right-icon" />
</Tag>
